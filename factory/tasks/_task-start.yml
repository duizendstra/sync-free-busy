# factory/tasks/_task-start.yml
version: '3'

tasks:
  task-start:
    desc: "Interactively or parametrically creates and checks out a new, conventional feature branch."
    silent: true
    cmds:
      # This now correctly passes the CLI arguments to the underlying script.
      - ./factory/scripts/start_task.sh {{.CLI_ARGS}}
