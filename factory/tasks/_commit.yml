# factory/tasks/_commit.yml
version: '3'

tasks:
  commit:
    desc: "Stages all changes and commits them with the provided arguments."
    silent: true
    cmds:
      # Call the script, passing all command-line arguments through.
      - ./factory/scripts/commit.sh {{.CLI_ARGS}}
